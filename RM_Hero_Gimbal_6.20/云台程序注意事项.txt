遥控器接收放在云台上
1.云台程序是从底盘程序中移植过来的
2.JY61陀螺仪需要测试  其中void JY61_Init(void)函数并没有在其他函数中调用，不知道具体的
作用。
3.为什么有2个串口传输JY61陀螺仪的数据（usart4和usart6）
4.各种中断的优先级没有设置
5.CAN1：云台pitch，yaw轴+传送电机
CAN2：拨齿+2个摩擦轮
6.云台控制模式中pid初始化，对于jy陀螺仪为什么有两种反馈模式（陀螺仪反馈和编码器反馈）
7.yaw需要2种pid用于跟随模式和分离模式
8.19年程序中云台保护
pit_protect_correct_1 = 0;
pit_protect_correct_2 = 8192;
有什么用（暂时没加）
9.此版本的云台控制暂时依然沿用上一届代码原理，（如果有大陀螺则yaw代码发生变化，需重新写）
/*就不存在跟随和分离模式*/
10.枪口热量限制代码在何处？！！！
/*热量限制*/
    if(remain_heat < 100)
      ptr_heat_gun_t.sht_flg = GunStop;
为什么这么少？！
(/*已解决：在底盘中已经有相关的程序将热量限制和当前热量做差比较，然后将差值给云台，也就是
remain_heat，当remain_heat小于100表示热量即将到达限制位置，马上停止摩擦轮，停止发射弹丸）
（是否可以优化代码，将热量限制的值发挥到最好？？？）
11.在枪管任务中
uint32_t run_time = 0;
uint8_t time_flag = 0;
变量用处是什么？？？
if(time_flag)
    {
      run_time++;
      GunReady = 0;
    }
    else GunReady = 1;
    if(run_time > 60)
    {
      time_flag = 0;
      run_time = 0;
    }
这段代码用处是什么？？？
12.新的拨弹代码程序运用于摩擦轮拨弹----（今年似乎又重新用拨齿拨弹，所以控制逻辑要更改）
13.2019代码中单发模式，如何进入位置环（这一次先设置标志位,然后需要下一次运行的时候才能进
入？为什么这么做？为什么这一次不直接进入）
还有，为什么没有设置连发逻辑？？？有点问题！！！